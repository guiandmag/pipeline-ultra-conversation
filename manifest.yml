---
declared-services:
  conversation-service:
    label: conversation
    plan: free
applications:
- name: unimed-demo-app
  command: npm start
  path: .
  memory: 512M
  instances: 1
  services:
  - Unimed-Bot
  env:
    NPM_CONFIG_PRODUCTION: false
